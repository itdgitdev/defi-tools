[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "defi-binance-futures-monitor"
version = "0.1.0"
requires-python = ">=3.13"
description = "Standalone Binance futures position monitor"

[tool.setuptools.packages.find]
where = ["src"]
